% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_vb_predictions.R
\name{plot_vb_predictions}
\alias{plot_vb_predictions}
\title{Plot von Bertalanffy Growth Model Predictions}
\usage{
plot_vb_predictions(
  predictions,
  original_data = NULL,
  show_points = TRUE,
  show_intervals = TRUE,
  facet_by_sex = NULL,
  alpha_ribbon = 0.3,
  alpha_points = 0.5,
  point_size = 0.9,
  line_size = 1,
  xlab = "Age",
  ylab = "Length (cm)",
  title = NULL
)
}
\arguments{
\item{predictions}{A data frame output from predict.vb_mle() containing predictions}

\item{original_data}{Optional data frame with original data points to overlay.
Should contain 'age', 'length', and optionally 'sex' columns.}

\item{show_points}{Logical. Whether to show original data points (default TRUE)}

\item{show_intervals}{Logical. Whether to show prediction/confidence intervals (default TRUE)}

\item{facet_by_sex}{Logical. Whether to create separate panels for each sex (default TRUE when sex present)}

\item{alpha_ribbon}{Numeric. Transparency for confidence/prediction intervals (default 0.3)}

\item{alpha_points}{Numeric. Transparency for data points (default 0.5)}

\item{point_size}{Numeric. Size of data points (default 0.9)}

\item{line_size}{Numeric. Size of fitted curve lines (default 1)}

\item{xlab}{Character. X-axis label (default "Age")}

\item{ylab}{Character. Y-axis label (default "Length (cm)")}

\item{title}{Character. Plot title (default NULL)}
}
\value{
A ggplot2 object
}
\description{
Creates a comprehensive plot showing the fitted von Bertalanffy growth curve(s),
prediction intervals, and original data points from predict.vb_mle() output.
}
\examples{
\dontrun{
# Fit model and make predictions
fit <- fit_vb_mle(age = age, length = length, sex = sex)
new_ages <- data.frame(
  age = rep(seq(0, 50, by = 1), each = 2),
  sex = rep(c("M", "F"), times = 51)
)
predictions <- predict(fit, newdata = new_ages, interval = "prediction")

# Create plot
plot_vb_predictions(predictions, original_data = fit$data)
}

}
