% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_vb_posteriors.R
\name{plot_vb_posteriors}
\alias{plot_vb_posteriors}
\title{Plot Posterior Predictive Checks for von Bertalanffy Bayesian Models}
\usage{
plot_vb_posteriors(model, ndraws = 50, theme_fn = ggplot2::theme_minimal())
}
\arguments{
\item{model}{A model object returned by fit_vb_brms()}

\item{ndraws}{Number of posterior draws to include (default 50)}

\item{theme_fn}{Optional ggplot2 theme function to apply (default theme_minimal)}
}
\value{
A list of ggplot2 objects
}
\description{
This function creates posterior predictive check plots for Bayesian von Bertalanffy growth models.
}
\examples{
\dontrun{
# Simple example with simulated data
age <- 1:15
length <- 100 * (1 - exp(-0.2 * (age - (-0.5)))) + rnorm(15, 0, 5)
fit <- fit_vb_brms(age = age, length = length)
plots <- plot_vb_posteriors(fit)
plots$posterior_predictive
}

}
